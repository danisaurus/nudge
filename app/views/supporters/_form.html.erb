<%= form_for([@user, @supporter]) do |f| %>
  <% if @supporter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@supporter.errors.count, "error") %> prohibited this supporter from being saved:</h2>

      <ul>
      <% @supporter.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class='block'>
    <%= f.text_field :first_name, :placeholder => 'first name', :class => 'borderRound'  %>
  </div >
  <div class='block'>
    <%= f.text_field :last_name, :placeholder => 'last name', :class => 'borderRound' %>
  </div>
  <div class='block'>
    <%= f.text_field :phone, :placeholder => 'phone', :class => 'borderRound' %>
  </div>

  <div class='block'>
    <%= f.submit 'add supporter' %>
  </div>

<% end %>
